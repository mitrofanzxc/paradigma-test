import { IPATHS } from './interface';

const PATHS: IPATHS = {
  main: '/',
  about: 'about',
  portfolio: 'portfolio',
  certificates: 'certificates',
  contacts: 'contacts',
  notFound: '*',
};

export { PATHS };
